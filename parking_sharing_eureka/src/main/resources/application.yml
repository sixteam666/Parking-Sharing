server:
  port: 9000
spring:
  application:
    name: parking_sharing_eureka_server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9000/eureka/
